**RULES**

--Dont's

1. Don't `run build` everytime there is a changes, run it only once i said it.
2. Don't `npm run dev` automatically, im doing it manually for checking the output.
3. Don't commit or push, I should only doing that.
4. Do not update automatically the `MEMORY.md` unless i say so.
5. Don't use Emooji icon, use shadcn icon.
6. Don't make the buttons, modal, alert, and many more in default style.

--Do's
1. Always depend on the `DESIGN.md` if you are going to implement something in user interface.
2. If i'm asking a suggestions or recommendations, always provide 3 or more options.
3. Read the `MEMORY.md` once per session so that you have context.
4. Use shadcn for UI component.
5. Before you proceed on adding or removing something, always create an implementation plan.